{
    "buildCommand": "npm install && npm run build",
    "outputDirectory": "./dist",
    "framework": "vite",
    "rewrites": [
        {
            "source": "/api/v1/(.*)",
            "destination": "../server/index.js"
        }
    ]
}