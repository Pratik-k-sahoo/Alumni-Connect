import { ReactTyped } from "react-typed";

const Heading = () => {
  return (
    <div>
    
        <ReactTyped strings={["Make a Donation 💌."]} typeSpeed={100} loop />
      
    </div>
  );
};

export default Heading;